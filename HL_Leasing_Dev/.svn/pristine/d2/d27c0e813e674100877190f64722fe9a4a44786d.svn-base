<?xml version="1.0" encoding="UTF-8"?>
<bm:model xmlns:bm="http://www.aurora-framework.org/schema/bm" needAccessControl="false">
    <bm:operations>
        <bm:operation name="query">
            <bm:query-sql><![CDATA[
                -- SELECT
                -- su.user_id,
                -- su.user_name
                -- FROM
                -- CFL_WFL_APPROVER_GROUP a,
                -- CFL_WF_APPROVER_RELATION b,
                -- zj_wfl_employee_vl zw,
                -- sys_user su
                -- WHERE
                -- a.approver_group_code IN ('CC', 'CA') AND
                -- a.enabled_flag         = 'Y' AND
                -- b.enabled_flag         = 'Y' AND
                -- a.approver_group_id    = b.approver_group_id AND
                -- b.approver_employee_id = zw.employee_id AND
                -- zw.employee_id         = su.employee_id
                
                -- SELECT
                    -- sg.user_id user_id,
                    -- su.description user_name
                -- FROM
                    -- sys_user_role_groups sg,
                    -- sys_user su
                -- WHERE
                    -- sg.role_id IN
                    -- (SELECT sr.role_id FROM sys_role sr WHERE sr.role_code IN ('0021', '0022')
                    -- ) AND
                    -- su.user_id = sg.user_id
                  select  s.user_id,s.description user_name from sys_user s where s.user_id in('1582','1583')
            ]]></bm:query-sql>
        </bm:operation>
    </bm:operations>
    <bm:fields>
        <bm:field name="user_id" databaseType="number" datatype="java.lang.Long" forDisplay="true" forQuery="true"/>
        <bm:field name="user_name" databaseType="VARCHAR2" datatype="java.lang.String" forDisplay="true" forQuery="true"/>
    </bm:fields>
</bm:model>
