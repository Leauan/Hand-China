<div ${id} class="${cls}" ${style}>
<input type="file" ${disabled} ${size} ${multiple} ${directory} ${filetype}/>
<div class="upload-list">
	<div><button class="btn">${text}</button></div>
	<ul></ul>
</div>
</div>
<script>
	new Touch.Uploader(${config});
</script>
${events}